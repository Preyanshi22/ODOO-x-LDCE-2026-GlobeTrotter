export function ProgressBar({ value, max = 100, tone = 'amber', label }: { value: number; max?: number; tone?: string; label?: string }) { const percent = Math.min(100, Math.max(0, (value / max) * 100)); return <div className="progress-wrap">{label && <div className="progress-meta"><span>{label}</span><strong>{Math.round(percent)}%</strong></div>}<div className="progress-track"><span className={`progress-fill progress-${tone}`} style={{ width: `${percent}%` }} /></div></div>; }
