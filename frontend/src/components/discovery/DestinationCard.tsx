import { ArrowUpRight, MapPin, Plus } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import type { Destination } from '../../types';
import { useApp } from '../../context/AppContext';
import { Button } from '../ui/Button';
import { SafeImage } from '../ui/Image';
export function DestinationCard({ destination, compact = false }: { destination: Destination; compact?: boolean }) { const { selectedTripId, addStop } = useApp(); const navigate = useNavigate(); return <article className={`destination-card ${compact ? 'destination-compact' : ''}`}><div className="destination-image-wrap"><SafeImage src={destination.image} alt={`${destination.city}, ${destination.country}`} className="destination-image" /><span className="image-badge">{destination.costIndex <= 2 ? 'Easy on budget' : destination.costIndex === 3 ? 'Mid-range' : 'Premium'}</span><button className="image-arrow" aria-label={`View ${destination.city}`} onClick={() => navigate(`/explore/cities/${destination.id}`)}><ArrowUpRight size={17} /></button></div><div className="destination-body"><div className="destination-kicker"><span><MapPin size={13} /> {destination.country}</span><span>♥ {destination.popularity}%</span></div><h3>{destination.city}</h3><p>{destination.description}</p>{!compact && <div className="destination-footer"><span className="tag-row">{destination.tags.slice(0, 2).map((tag) => <span className="tag" key={tag}>{tag}</span>)}</span><Button variant="secondary" icon={<Plus size={15} />} onClick={() => selectedTripId ? addStop(selectedTripId, destination) : navigate('/trips/new')}>Add to trip</Button></div>}</div></article>; }
