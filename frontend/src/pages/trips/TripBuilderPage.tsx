import { ArrowLeft, ArrowRight, CalendarRange, Check, MapPin, Plus, Share2, Sparkles } from 'lucide-react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { StopCard } from '../../components/trips/StopCard';
import { Button } from '../../components/ui/Button';
import { Modal } from '../../components/ui/Modal';
import { useApp, formatDateRange, formatMoney, tripDays } from '../../context/AppContext';
export function TripBuilderPage() { const { id } = useParams(); const navigate = useNavigate(); const { trips, selectedTrip, destinations, activities, setSelectedTripId, addStop, updateStop, removeStop, moveStop, addActivity, removeActivity, addToast } = useApp(); const trip = trips.find((item) => item.id === id) ?? selectedTrip; const [modal, setModal] = useState<'stop' | 'activity' | null>(null); const [stopForActivity, setStopForActivity] = useState(''); useEffect(() => { if (trip && selectedTrip?.id !== trip.id) setSelectedTripId(trip.id); }, [trip, selectedTrip?.id, setSelectedTripId]); if (!trip) return <div className="loading-screen"><p>Finding that trip...</p></div>; const totalActivityCost = trip.stops.flatMap((stop) => stop.activities).reduce((sum, activity) => sum + activity.price, 0); return <div className="builder-page"><Link to="/trips" className="back-link"><ArrowLeft size={16} /> All trips</Link><div className="builder-hero"><div><span className="eyebrow">Your route, your rhythm</span><h1>{trip.name}</h1><p>{trip.description}</p><div className="builder-meta"><span><CalendarRange size={15} /> {formatDateRange(trip.startDate, trip.endDate)}</span><span><MapPin size={15} /> {trip.stops.length} {trip.stops.length === 1 ? 'stop' : 'stops'}</span><span><Sparkles size={15} /> {tripDays(trip)} days</span></div></div><div className="builder-actions"><Button variant="secondary" icon={<Share2 size={16} />} onClick={() => { navigator.clipboard?.writeText(`${window.location.origin}/shared/${trip.id}`); addToast('Share link copied.', 'info'); }}>Share</Button><Button icon={<Check size={16} />} onClick={() => navigate(`/trips/${trip.id}/view`)}>Finish planning</Button></div></div><div className="builder-stepper"><span className="completed"><b>01</b> Trip details <Check size={14} /></span><i /><span className="active"><b>02</b> Destinations</span><i /><span><b>03</b> Activities</span><i /><span><b>04</b> Review</span></div><div className="builder-summary"><div><span className="eyebrow">A route in progress</span><div className="route-line">{trip.stops.length ? trip.stops.map((stop, index) => <span key={stop.id}><strong>{stop.city}</strong>{index < trip.stops.length - 1 && <i>→</i>}</span>) : <span className="muted">Add your first stop to begin</span>}</div></div><div className="builder-stat"><span>Estimated total</span><strong>{formatMoney(trip.budget.total + totalActivityCost)}</strong></div></div><div className="builder-content"><section className="stops-column"><div className="section-heading"><div><p className="eyebrow">The journey</p><h2>Your stops</h2></div><Button variant="secondary" icon={<Plus size={15} />} onClick={() => setModal('stop')}>Add stop</Button></div>{trip.stops.length === 0 ? <div className="card builder-empty"><div className="empty-icon"><MapPin size={22} /></div><h3>Every great route starts somewhere.</h3><p>Choose your first city and begin connecting the dots.</p><Button onClick={() => setModal('stop')}>Add your first stop</Button></div> : <div className="stops-list">{trip.stops.map((stop, index) => <StopCard key={stop.id} stop={stop} index={index} total={trip.stops.length} onRemove={() => removeStop(trip.id, stop.id)} onMove={(direction) => moveStop(trip.id, stop.id, direction)} onAddActivity={() => { setStopForActivity(stop.id); setModal('activity'); }} onRemoveActivity={(activityId) => removeActivity(trip.id, stop.id, activityId)} onDatesChange={(dates) => updateStop(trip.id, stop.id, dates)} />)}</div>}</section><aside className="builder-aside"><div className="card next-step-card"><div className="next-step-icon"><Sparkles size={18} /></div><span className="eyebrow">Next up</span><h3>Fill your days with moments</h3><p>Find activities that turn a destination into a memory.</p><Button className="button-wide" variant="dark" onClick={() => { setStopForActivity(trip.stops[0]?.id ?? ''); setModal('activity'); }}>Browse experiences <ArrowRight size={15} /></Button></div><div className="card builder-budget-card"><div className="section-heading"><h3>Budget snapshot</h3><Link to={`/budget/${trip.id}`}><ArrowRight size={15} /></Link></div><strong>{formatMoney(trip.budget.total)}</strong><p className="muted">Estimated trip budget</p><div className="mini-breakdown">{Object.entries(trip.budget.categories).slice(0, 4).map(([name, value]) => <span key={name}><i /> {name}<b>{formatMoney(value)}</b></span>)}</div></div></aside></div><Modal open={modal === 'stop'} title="Add a destination" onClose={() => setModal(null)}><p className="modal-copy">Where would you like to go next?</p><div className="modal-destination-grid">{destinations.filter((destination) => !trip.stops.some((stop) => stop.city === destination.city)).slice(0, 6).map((destination) => <button key={destination.id} className="modal-destination" onClick={() => { addStop(trip.id, destination); setModal(null); }}><img src={destination.image} alt="" /><span><strong>{destination.city}</strong><small>{destination.country}</small></span><Plus size={15} /></button>)}</div><p className="modal-hint">Tip: use Explore for more ideas.</p></Modal><Modal open={modal === 'activity'} title="Add an experience" onClose={() => setModal(null)}><p className="modal-copy">Choose something to make {trip.stops.find((stop) => stop.id === stopForActivity)?.city ?? 'this stop'} feel like yours.</p><div className="modal-activity-list">{activities.slice(0, 5).map((activity) => <div key={activity.id} className="modal-activity"><img src={activity.image} alt="" /><div><strong>{activity.name}</strong><small>{activity.city} · {formatMoney(activity.price)}</small></div><Button variant="secondary" onClick={() => { if (stopForActivity) addActivity(trip.id, stopForActivity, activity); setModal(null); }}>Add</Button></div>)}</div></Modal></div>; }
