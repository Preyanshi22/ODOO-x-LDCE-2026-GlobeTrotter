import { ArrowLeft, CalendarDays, ChevronLeft, ChevronRight, Clock3, Copy, MapPin, Share2, Train, Utensils, Wallet } from 'lucide-react';
import { useMemo, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { Badge } from '../../components/ui/Badge';
import { Button } from '../../components/ui/Button';
import { SafeImage } from '../../components/ui/Image';
import { formatDateRange, formatMoney, tripDays, useApp } from '../../context/AppContext';
export function ItineraryViewPage({ shared = false }: { shared?: boolean }) { const { id } = useParams(); const { selectedTrip, trips, copyTrip, addToast } = useApp(); const trip = trips.find((item) => item.id === id) ?? selectedTrip; const [mode, setMode] = useState<'timeline' | 'list'>('timeline'); const [day, setDay] = useState(1); const allActivities = trip?.stops.flatMap((stop) => stop.activities.map((activity) => ({ ...activity, stop }))) ?? []; const dayActivities = useMemo(() => allActivities.filter((activity) => activity.day === day), [allActivities, day]); if (!trip) return <div className="loading-screen"><p>Loading itinerary...</p></div>; return <div className="itinerary-page"><Link to={shared ? '/' : `/trips/${trip.id}/build`} className="back-link"><ArrowLeft size={16} /> {shared ? 'GlobeTrotter' : 'Back to builder'}</Link><section className="itinerary-cover"><SafeImage src={trip.cover} alt={trip.name} /><div className="itinerary-cover-overlay"><div><span className="eyebrow light">{shared ? 'A shared journey' : 'Your finished itinerary'}</span><h1>{trip.name}</h1><p>{trip.stops.map((stop) => stop.city).join('  ·  ') || 'A journey waiting to happen'} <span>•</span> {formatDateRange(trip.startDate, trip.endDate)}</p></div><div className="itinerary-cover-actions">{!shared && <Link to={`/trips/${trip.id}/build`} className="button button-light">Edit itinerary</Link>}{shared && <Button variant="light" icon={<Copy size={16} />} onClick={() => { const copy = copyTrip(trip.id); if (copy) window.location.href = `/trips/${copy.id}/build`; }}>Copy trip</Button>}<Button variant="light" icon={<Share2 size={16} />} onClick={() => { navigator.clipboard?.writeText(window.location.href); addToast('Itinerary link copied.', 'info'); }}>Share</Button></div></div></section><div className="itinerary-summary"><div><span className="summary-icon"><CalendarDays size={18} /></span><span><small>Duration</small><strong>{tripDays(trip)} days</strong></span></div><div><span className="summary-icon"><MapPin size={18} /></span><span><small>Destinations</small><strong>{trip.stops.length} cities</strong></span></div><div><span className="summary-icon"><Wallet size={18} /></span><span><small>Estimated budget</small><strong>{formatMoney(trip.budget.total)}</strong></span></div><div className="summary-spacer" /><div className="view-toggle" role="group" aria-label="Itinerary view"><button className={mode === 'timeline' ? 'active' : ''} onClick={() => setMode('timeline')}>Timeline</button><button className={mode === 'list' ? 'active' : ''} onClick={() => setMode('list')}>List</button></div></div><div className="itinerary-body"><aside className="day-nav"><div className="day-nav-heading"><span className="eyebrow">Your days</span><span>{tripDays(trip)} total</span></div>{Array.from({ length: Math.min(tripDays(trip), 12) }, (_, index) => index + 1).map((number) => <button key={number} className={day === number ? 'active' : ''} onClick={() => setDay(number)}><span>Day {String(number).padStart(2, '0')}</span><small>{dayLabel(trip, number)}</small><ChevronRight size={15} /></button>)}</aside><main className={`day-itinerary ${mode}`}><div className="day-title-row"><div><span className="eyebrow">Day {String(day).padStart(2, '0')}</span><h2>{dayLabel(trip, day)}</h2><p className="muted">{dayActivities.length ? `${dayActivities.length} moments planned` : 'A little room for spontaneity'}</p></div><div className="day-nav-arrows"><Button variant="ghost" disabled={day <= 1} onClick={() => setDay((value) => value - 1)}><ChevronLeft size={16} /></Button><Button variant="ghost" disabled={day >= tripDays(trip)} onClick={() => setDay((value) => value + 1)}><ChevronRight size={16} /></Button></div></div><div className="day-location"><span className="location-line" /><MapPin size={15} /><strong>{dayActivities[0]?.stop.city ?? trip.stops[Math.min(day - 1, trip.stops.length - 1)]?.city ?? 'Open day'}</strong><span>{dayActivities[0]?.stop.country ?? 'Explore at your own pace'}</span></div>{dayActivities.length ? <div className="activity-timeline">{dayActivities.sort((a, b) => a.time.localeCompare(b.time)).map((item) => <div className="timeline-item" key={item.id}><div className="timeline-time">{item.time}<small>{item.duration}</small></div><div className="timeline-rail"><span /><i /></div><article className="timeline-card"><SafeImage src={item.image} alt={item.name} /><div><div className="timeline-card-top"><Badge tone="amber">{item.category}</Badge><span>{formatMoney(item.price)} / person</span></div><h3>{item.name}</h3><p>{item.description}</p><div className="timeline-extra"><span><Clock3 size={13} /> {item.duration}</span><span><MapPin size={13} /> {item.stop.city}</span></div></div></article></div>)}</div> : <div className="day-empty"><div className="empty-icon">✦</div><h3>Leave a little room for wonder</h3><p>No activities are pinned to this day yet. That can be intentional.</p></div>}<div className="travel-note"><Train size={18} /><div><strong>Travel note</strong><p>Remember to leave a little buffer between each beautiful thing.</p></div><Utensils size={18} /></div></main></div></div>; }
function dayLabel(trip: NonNullable<ReturnType<typeof useApp>['selectedTrip']>, day: number) { if (!trip) return 'Open day'; const date = new Date(new Date(`${trip.startDate}T12:00:00`).getTime() + (day - 1) * 86400000); return date.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' }); }
