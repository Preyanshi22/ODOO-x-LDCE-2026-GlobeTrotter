import { Camera, Check, Globe2, Lock, Mail, MapPin, Phone, Save, Shield, Trash2, UserRound } from 'lucide-react';
import { FormEvent, useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { Button } from '../../components/ui/Button';
import { Modal } from '../../components/ui/Modal';
import { SafeImage } from '../../components/ui/Image';
import { useApp } from '../../context/AppContext';
export function ProfilePage() { const { profile, updateProfile, addToast } = useApp(); const [section, setSection] = useState<'profile' | 'preferences' | 'privacy'>('profile'); const [deleteOpen, setDeleteOpen] = useState(false); const [form, setForm] = useState(profile); useEffect(() => { if (profile) setForm(profile); }, [profile]); if (!profile || !form) return <div className="loading-screen" />; const set = (key: keyof typeof form) => (event: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => setForm((current) => ({ ...current, [key]: event.target.value })); const save = (event: FormEvent) => { event.preventDefault(); updateProfile(form); addToast('Your profile was saved.'); }; return <div className="profile-page"><div className="page-heading-row"><div><p className="eyebrow">Your place in the world</p><h1>Profile & settings</h1><p className="lede">Keep your details and preferences in step with you.</p></div></div><div className="profile-layout"><aside className="profile-nav card"><div className="profile-card-top"><div className="profile-avatar-wrap"><SafeImage src={form.avatar} alt={`${form.firstName} ${form.lastName}`} /><button aria-label="Change profile photo"><Camera size={14} /></button></div><h2>{form.firstName} {form.lastName}</h2><p>{form.email}</p></div><nav>{[['profile', <UserRound size={16} />, 'Personal details'], ['preferences', <Globe2 size={16} />, 'Travel preferences'], ['privacy', <Shield size={16} />, 'Privacy & security']].map(([value, icon, label]) => <button key={String(value)} className={section === value ? 'active' : ''} onClick={() => setSection(value as typeof section)}>{icon}<span>{label}</span></button>)}</nav><Link to="/login" className="logout-link">Sign out</Link></aside><main className="card profile-form-card">{section === 'profile' && <form onSubmit={save}><div className="form-card-heading"><div className="form-icon"><UserRound size={20} /></div><div><h2>Personal details</h2><p>This is how you’ll appear to other travellers.</p></div></div><div className="form-grid form-grid-two"><div className="field"><label htmlFor="profile-first">First name</label><input id="profile-first" value={form.firstName} onChange={set('firstName')} /></div><div className="field"><label htmlFor="profile-last">Last name</label><input id="profile-last" value={form.lastName} onChange={set('lastName')} /></div><div className="field"><label htmlFor="profile-email">Email address</label><div className="input-icon"><Mail size={16} /><input id="profile-email" type="email" value={form.email} onChange={set('email')} /></div></div><div className="field"><label htmlFor="profile-phone">Phone number</label><div className="input-icon"><Phone size={16} /><input id="profile-phone" value={form.phone} onChange={set('phone')} /></div></div><div className="field"><label htmlFor="profile-city">Home city</label><div className="input-icon"><MapPin size={16} /><input id="profile-city" value={form.city} onChange={set('city')} /></div></div><div className="field"><label htmlFor="profile-country">Country</label><input id="profile-country" value={form.country} onChange={set('country')} /></div></div><div className="field"><label htmlFor="profile-avatar">Profile photo URL</label><input id="profile-avatar" value={form.avatar} onChange={set('avatar')} /></div><div className="form-submit-row"><span><Check size={15} /> Changes save on this device</span><Button type="submit" icon={<Save size={15} />}>Save changes</Button></div></form>}{section === 'preferences' && <form onSubmit={save}><div className="form-card-heading"><div className="form-icon"><Globe2 size={20} /></div><div><h2>Travel preferences</h2><p>Help us make future recommendations feel more like you.</p></div></div><div className="preference-block"><h3>Favorite ways to travel</h3><div className="preference-chips">{['Slow mornings', 'Local food', 'Art & culture', 'Nature', 'Nightlife', 'Wellness', 'Architecture', 'Road trips'].map((item) => <button type="button" className={item === 'Local food' || item === 'Art & culture' ? 'selected' : ''} key={item}>{item}{(item === 'Local food' || item === 'Art & culture') && <Check size={14} />}</button>)}</div></div><div className="field"><label htmlFor="language">Language</label><select id="language" value={form.language} onChange={set('language')}><option>English (US)</option><option>English (UK)</option><option>हिन्दी</option><option>Español</option></select></div><div className="form-submit-row"><span>Recommendations are always optional.</span><Button type="submit" icon={<Save size={15} />}>Save preferences</Button></div></form>}{section === 'privacy' && <div><div className="form-card-heading"><div className="form-icon"><Shield size={20} /></div><div><h2>Privacy & security</h2><p>You’re in control of what you share and who sees it.</p></div></div><div className="settings-list"><div><span className="settings-icon"><Lock size={17} /></span><span><strong>Profile visibility</strong><small>Choose who can see your shared trips</small></span><select value={form.privacy} onChange={set('privacy')}><option value="public">Everyone</option><option value="friends">Friends only</option><option value="private">Only me</option></select></div><div><span className="settings-icon"><Mail size={17} /></span><span><strong>Travel notes & updates</strong><small>Occasional inspiration in your inbox</small></span><label className="switch"><input type="checkbox" defaultChecked /><span /></label></div><div><span className="settings-icon"><Shield size={17} /></span><span><strong>Two-step verification</strong><small>Add another layer of account security</small></span><button className="button button-secondary">Set up</button></div></div><div className="danger-zone"><div><h3>Delete account</h3><p>This permanently removes your profile and saved trips.</p></div><Button variant="danger" icon={<Trash2 size={15} />} onClick={() => setDeleteOpen(true)}>Delete account</Button></div></div>}</main></div><Modal open={deleteOpen} title="Delete your account?" onClose={() => setDeleteOpen(false)}><p className="modal-copy">This demo will only clear your local profile. Are you sure?</p><div className="modal-actions"><Button variant="ghost" onClick={() => setDeleteOpen(false)}>Cancel</Button><Button variant="danger" onClick={() => { setDeleteOpen(false); addToast('Account deletion is disabled in the demo.', 'info'); }}>Delete account</Button></div></Modal></div>; }
